# CLAUDE.md

Read `AGENTS.md` before planning or modifying code.

When working in a nested application or package, also read its local
`AGENTS.md` and `CLAUDE.md`, if present.

Do not assume the backend stack until the decision is recorded in
`docs/project-plan.md` and `docs/development/todo.md`.
